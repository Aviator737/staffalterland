<%= form_for(task_image) do |f| %>
  <% if task_image.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task_image.errors.count, "error") %> prohibited this task_image from being saved:</h2>

      <ul>
      <% task_image.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task_id %>
    <%= f.number_field :task_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= f.text_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
